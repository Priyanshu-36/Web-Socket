WebSocket is a protocol that enables real-time, two-way communication between a client and a server over a single persistent TCP connection. It starts with an HTTP Upgrade request, the server responds with 101 Switching Protocols, and then the connection remains open. Unlike HTTP, where the client must initiate every request, WebSocket allows both the client and server to send messages at any time, making it ideal for chat applications, live updates, and online gaming."
